<div class="container">
  <h1>My tasks</h1>
  <ul>
    <% @tasks.each do |task|  %>
      <li><%= task.completed ? "[X]" : "[ ]" %> <%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
  <%= link_to "Nueva task?", new_task_path, class: 'btn' %>
</div>
